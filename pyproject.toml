[project]
name = "playground-platform-python-tools"
version = "0.0.0"
description = "Python tooling for playground-platform"
requires-python = ">=3.11"
dependencies = [
  "networkx==3.5",
  "python-louvain==0.16",
]

[tool.uv]
package = false
